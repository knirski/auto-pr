# 180 days inactive = stale, 180 more days = close (360 days total).
name: Stale

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:

concurrency:
  group: stale
  cancel-in-progress: true

permissions: {}

jobs:
  stale:
    permissions:
      issues: write
      pull-requests: write
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Mark stale issues and PRs
        uses: actions/stale@1e223db275d687790206a7acac4d1a11bd6fe629 # v10.4.0
        with:
          stale-issue-message: >
            This issue has been automatically marked as stale because it has not had
            recent activity. It will be closed in 180 days if no further activity occurs.
            Thank you for your contributions.
          close-issue-message: >
            This issue was closed due to inactivity. Feel free to reopen if the
            issue persists.
          stale-pr-message: >
            This pull request has been automatically marked as stale because it has
            not had recent activity. It will be closed in 180 days if no further
            activity occurs.
          close-pr-message: >
            This pull request was closed due to inactivity. Feel free to reopen if
            you'd like to continue.
          days-before-stale: 180
          days-before-close: 180
          exempt-issue-labels: "pinned,security"
          exempt-pr-labels: "pinned,security"

# Review fix: execute stale-source skip paths

1. Add a reusable mocked-`gh` fixture for source-validation responses.
2. Execute missing-branch, changed-tip, expired-commit, and same-repository-PR cases.
3. Run focused tests, the full check, commit, push, and report the results.

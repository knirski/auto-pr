title = "auto-pr"

[extend]
useDefault = true

[allowlist]
description = "Documented env placeholders; skip dependency and build output trees"
paths = [
	'''\.env\.example$''',
	'''(^|/)node_modules(/|$)''',
	'''(^|/)dist(/|$)''',
	'''(^|/)coverage(/|$)''',
	'''(^|/)\.worktrees(/|$)''',
]

# Git hooks via Lefthook. See https://lefthook.dev/

pre-commit:
  jobs:
    - run: npx lint-staged
    - glob: "*.nix"
      run: npm run check:nix

commit-msg:
  jobs:
    - run: npx --no -- commitlint --edit {1}

pre-push:
  jobs:
    - run: npm run check:code
